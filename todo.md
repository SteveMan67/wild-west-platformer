# **High Priority**
- if the user isn't signed in, make ux so they can save their level
- allow resizing of the level (I already have a function for that)
- drag triggers around
- stop number keys switching blocks while the menu is open
- fix adjacency calculations for non adjacency calculating blocks, still calculate for the tiles around it.
- only allow one start flag in selection fill
- fix tile not switching back after erase / undefined error associated with it
- fix trigger not going away after delteted
- add delay to trigger
- add dissipation animation
- only highlight selected trigger (if any) and have different colors for different operations

# Finishing Touches
- convert tiles to spritesheet
- run es build so the browser doesn't have to make so many fetches
